{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.bitgett/q402-mcp",
  "description": "Q402 — gasless USDC, USDT, and RLUSD payments across 9 EVM chains, callable from any MCP client.",
  "repository": {
    "url": "https://github.com/bitgett/q402-mcp",
    "source": "github"
  },
  "version": "0.5.4",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@quackai/q402-mcp",
      "version": "0.5.4",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "Q402_TRIAL_API_KEY",
          "description": "Free Trial API key from https://q402.quackai.ai/event. BNB Chain only, 2,000 sponsored TXs, gas covered by Q402. BNB payments auto-route to this key when set (same rule for q402_pay and q402_batch_pay up to 5 recipients). For 6+ recipient BNB batches, q402_batch_pay returns status='ambiguous' so the agent can ask the user how to split.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_MULTICHAIN_API_KEY",
          "description": "Paid Multichain API key from https://q402.quackai.ai/payment. Full 9-chain support with per-chain Gas Tank. Auto-routed for non-BNB payments AND whenever keyScope='multichain' is chosen. Use it to send all rows in a 6+ recipient BNB batch after the ambiguity prompt.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_API_KEY",
          "description": "Legacy single-env fallback. Used for both scopes when neither Q402_TRIAL_API_KEY nor Q402_MULTICHAIN_API_KEY is set. q402_test_* keys keep sandbox mode; q402_live_* keys are required for real on-chain transactions.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_PRIVATE_KEY",
          "description": "Hex-encoded EVM private key used to sign payment authorizations. Required only for real on-chain payments (live mode).",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_ENABLE_REAL_PAYMENTS",
          "description": "Set to '1' to allow real on-chain transactions. Any other value (or unset) keeps the server in sandbox mode regardless of API key tier.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "0"
        },
        {
          "name": "Q402_MAX_AMOUNT_PER_CALL",
          "description": "Per-call USD-equivalent cap. Any q402_pay request with amount above this is rejected before signing.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "5"
        },
        {
          "name": "Q402_ALLOWED_RECIPIENTS",
          "description": "Optional comma-separated lowercase EVM addresses. When set, q402_pay rejects any recipient not on this allowlist.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "Q402_RELAY_BASE_URL",
          "description": "Override for the Q402 relay endpoint. Defaults to https://q402.quackai.ai/api. Set explicitly when running against a self-hosted Q402 deployment or a non-canonical environment.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "https://q402.quackai.ai/api"
        }
      ]
    }
  ]
}
