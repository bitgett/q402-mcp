{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.bitgett/q402-mcp",
  "description": "Q402 — gasless USDC, USDT, and RLUSD payments across 7 EVM chains, callable from any MCP client.",
  "repository": {
    "url": "https://github.com/bitgett/q402-mcp",
    "source": "github"
  },
  "version": "0.3.13",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@quackai/q402-mcp",
      "version": "0.3.13",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "Q402_API_KEY",
          "description": "Q402 API key from https://q402.quackai.ai/dashboard. q402_test_* keys keep the server in sandbox mode; q402_live_* keys are required for real on-chain transactions.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_PRIVATE_KEY",
          "description": "Hex-encoded EVM private key used to sign payment authorizations. Required only for real on-chain payments (live mode).",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "Q402_ENABLE_REAL_PAYMENTS",
          "description": "Set to '1' to allow real on-chain transactions. Any other value (or unset) keeps the server in sandbox mode regardless of API key tier.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "0"
        },
        {
          "name": "Q402_MAX_AMOUNT_PER_CALL",
          "description": "Per-call USD-equivalent cap. Any q402_pay request with amount above this is rejected before signing.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "5"
        },
        {
          "name": "Q402_ALLOWED_RECIPIENTS",
          "description": "Optional comma-separated lowercase EVM addresses. When set, q402_pay rejects any recipient not on this allowlist.",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "Q402_RELAY_BASE_URL",
          "description": "Override for the Q402 relay endpoint. During the BNB-focus sprint (0.3.13) the default points at the sprint preview deploy so trial users hit the same KV as the dashboard issuing their keys; this flips back to https://q402.quackai.ai/api in 0.4.0 post-merge. Set explicitly if running against a self-hosted Q402 deployment.",
          "isRequired": false,
          "isSecret": false,
          "format": "string",
          "default": "https://q402-institutional-git-feat-bnb-f-e317ee-bitgett-7677s-projects.vercel.app/api"
        }
      ]
    }
  ]
}
